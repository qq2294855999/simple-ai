<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.task.TaskRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into task(id, agent_memory_id, task_name, execution_status, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.agentMemoryId}, #{entity.taskName}, #{entity.executionStatus}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
</mapper>

