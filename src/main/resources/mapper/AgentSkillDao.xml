<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.agentSkill.AgentSkillRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into agent_skill(id, name, definition_desc, status, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.name}, #{entity.definitionDesc}, #{entity.status}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
</mapper>

