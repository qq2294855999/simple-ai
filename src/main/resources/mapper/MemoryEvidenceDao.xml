<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.memoryEvidence.MemoryEvidenceRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into memory_evidence(id, turn_id, memory_version_id, evidence_type, evidence_content, quality_score, create_time, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.turnId}, #{entity.memoryVersionId}, #{entity.evidenceType}, #{entity.evidenceContent}, #{entity.qualityScore}, #{entity.createTime}, #{entity.status})
        </foreach>
    </insert>
</mapper>

