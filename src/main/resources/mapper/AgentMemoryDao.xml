<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.agentMemory.AgentMemoryRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into agent_memory(id, agent_id, memory_name, trigger_condition, status, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.agentId}, #{entity.memoryName}, #{entity.triggerCondition}, #{entity.status}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
</mapper>

