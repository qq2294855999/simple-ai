<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.agentMemory.AgentMemoryRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into agent_memory(id, agent_id, memory_name, step_name, trigger_condition, trigger_action, create_time, update_time, status, reserver, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.agentId}, #{entity.memoryName}, #{entity.stepName}, #{entity.triggerCondition}, #{entity.triggerAction}, #{entity.createTime}, #{entity.updateTime},
            #{entity.status}, #{entity.reserver}, #{entity.remark})
        </foreach>
    </insert>
</mapper>

