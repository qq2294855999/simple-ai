<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.agentMemoryStep.AgentMemoryStepRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into agent_memory_step(id, memory_id, sequence_no, atomic_command_id, atomic_command_code, step_name, args_template,
        delay_min_ms, delay_max_ms, timeout_ms, success_assertion, failure_strategy, status, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.memoryId}, #{entity.sequenceNo}, #{entity.atomicCommandId}, #{entity.atomicCommandCode},
            #{entity.stepName}, #{entity.argsTemplate}, #{entity.delayMinMs}, #{entity.delayMaxMs}, #{entity.timeoutMs},
            #{entity.successAssertion}, #{entity.failureStrategy}, #{entity.status}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

</mapper>