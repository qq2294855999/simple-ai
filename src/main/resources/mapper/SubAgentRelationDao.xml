<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.subAgentRelation.SubAgentRelationRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sub_agent_relation(id, main_agent_id, sub_agent_id, create_time, update_time, status, reserver, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.mainAgentId}, #{entity.subAgentId}, #{entity.createTime}, #{entity.updateTime}, #{entity.status}, #{entity.reserver}, #{entity.remark})
        </foreach>
    </insert>
</mapper>

