<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.ai.view.atomicCommand.AtomicCommandRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into atomic_command(id, name, command, role, skill_id, create_time, update_time, status, reserver, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.name}, #{entity.command}, #{entity.role}, #{entity.skillId}, #{entity.createTime}, #{entity.updateTime}, #{entity.status}, #{entity.reserver}, #{entity.remark})
        </foreach>
    </insert>
</mapper>

